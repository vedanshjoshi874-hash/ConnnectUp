services:
  - type: web
    name: connectup-frontend
    env: static
    buildCommand: cd frontend && npm ci && npm run build && cd .. && cp -r frontend/dist build
    staticPublishPath: build
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
